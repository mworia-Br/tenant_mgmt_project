name: CI/CD

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build and Push Docker Images
        run: docker build . -t ghcr.io/user/app && docker push ghcr.io/user/app
